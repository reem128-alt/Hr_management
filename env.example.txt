# ============================================
# HR Management System - Environment Variables
# ============================================
# 
# Copy this file to .env and update with your actual values
# DO NOT commit the .env file to version control
#
# ============================================

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL connection string
# Replace the following placeholders:
#   - postgres: your database username
#   - password: your database password
#   - localhost: your database host (use 'localhost' for local, or IP/domain for remote)
#   - 5432: your PostgreSQL port (default is 5432)
#   - hr_management: your database name
DATABASE_URL="postgresql://postgres:password@localhost:5432/hr_management?schema=public"

# Alternative format for remote database:
# DATABASE_URL="postgresql://username:password@your-host.com:5432/hr_management?schema=public"

# For cloud databases (e.g., AWS RDS, Heroku Postgres, etc.):
# DATABASE_URL="postgresql://user:pass@host.region.rds.amazonaws.com:5432/dbname?schema=public"

# ============================================
# JWT AUTHENTICATION
# ============================================
# Secret key for signing and verifying JWT tokens
# IMPORTANT: Use a strong, random string in production!
# You can generate one using: openssl rand -base64 32
# Or online: https://randomkeygen.com/
JWT_SECRET="change-this-to-a-strong-random-secret-key-in-production"

# JWT token expiration time
# Format: number followed by unit (s=seconds, m=minutes, h=hours, d=days)
# Examples:
#   "15m"  = 15 minutes
#   "1h"   = 1 hour
#   "24h"  = 24 hours
#   "7d"   = 7 days (default)
#   "30d"  = 30 days
JWT_EXPIRES_IN="7d"

# ============================================
# APPLICATION SETTINGS
# ============================================
# Port number where the NestJS application will run
# Default: 3000
PORT=3000

# Environment mode
# Options: development, production, test
NODE_ENV=development

# ============================================
# OPTIONAL CONFIGURATIONS
# ============================================

# Database Connection Pool (uncomment if needed)
# DB_POOL_MIN=2
# DB_POOL_MAX=10
# DB_POOL_IDLE_TIMEOUT=30000

# CORS Origins (comma-separated list of allowed origins)
# CORS_ORIGIN=http://localhost:3000,http://localhost:4200,https://yourdomain.com

# File Upload Settings (if implementing file uploads)
# MAX_FILE_SIZE=10485760
# UPLOAD_DEST=./uploads
# ALLOWED_FILE_TYPES=jpg,jpeg,png,pdf,doc,docx

# Email Configuration (if implementing email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@yourcompany.com

# Redis Configuration (if using Redis for caching/sessions)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Logging Level
# LOG_LEVEL=debug

